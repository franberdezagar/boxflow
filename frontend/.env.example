# Desarrollo local: apuntar al backend local
VITE_API_URL=http://localhost:3001/api

# En producción (Vercel) NO definas VITE_API_URL: el frontend usará
# automáticamente el mismo dominio (window.location.origin + /api),
# que resuelve a la función serverless /api.
